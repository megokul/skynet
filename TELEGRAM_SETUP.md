# Telegram Setup

Telegram runtime is owned by OpenClaw, not SKYNET.

Use OpenClaw gateway setup:
- `openclaw-gateway/main.py`

Relevant env vars:
- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_ALLOWED_USER_ID`
- `DISABLE_TELEGRAM_BOT`

SKYNET remains control-plane only and does not host user channels.
