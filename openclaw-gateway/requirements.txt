# SKYNET Gateway — Python dependencies
#
# Install:  pip install -r requirements.txt

# WebSocket server (worker laptop connection)
websockets>=14.0,<15.0

# HTTP API (internal loopback dispatch)
aiohttp>=3.10,<4.0

# Telegram bot
python-telegram-bot>=21.0,<22.0

# Database
aiosqlite>=0.20.0,<1.0

# AI providers
google-genai>=1.0
groq>=0.8.0
openai>=1.0
anthropic>=0.30.0

# HTTP client (AI API calls, web search)
httpx>=0.27.0

# SSH log mirror + SSH tunnel fallback (optional)
paramiko>=3.4.0
